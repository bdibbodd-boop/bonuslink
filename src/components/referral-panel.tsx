"use client";
import { useState } from "react";
export function ReferralPanel({ code, siteUrl }: { code: string; siteUrl: string }) { const [copied, setCopied] = useState(false); const link = `${siteUrl}/inscription?ref=${code}`; async function copy() { await navigator.clipboard.writeText(link); setCopied(true); } return <div className="grid gap-4"><div className="flex items-center justify-between border border-[#d9d7cb] bg-[#fffdf6] p-4"><div><p className="eyebrow">Votre code</p><p className="mt-2 text-2xl font-bold tracking-widest">{code}</p></div><button onClick={copy} className="sans rounded-full bg-[#17211b] px-4 py-2 text-sm font-bold text-[#f4f1e8]">{copied ? "Copié" : "Copier le lien"}</button></div><p className="sans break-all text-sm text-[#66705f]">{link}</p><div className="flex gap-3"><a className="sans rounded-full border border-[#17211b] px-4 py-2 text-sm font-bold" href={`https://wa.me/?text=${encodeURIComponent(`Rejoins-moi sur BonusLink : ${link}`)}`} target="_blank" rel="noreferrer">Partager WhatsApp</a><a className="sans rounded-full border border-[#17211b] px-4 py-2 text-sm font-bold" href={`mailto:?subject=BonusLink&body=${encodeURIComponent(link)}`}>Partager par email</a></div></div>; }
